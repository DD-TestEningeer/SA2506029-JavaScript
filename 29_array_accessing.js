


// create an array of number elements 

let numbers = [11, 22, 33, 44];

// to add the element in the end of array
numbers.push(55);

console.log(numbers);

// to calculate the length of array
console.log(numbers.length);

// to remove the values from array
numbers.pop();
console.log(numbers);

// using index 
console.log(numbers.at(0));
